{
  "name": "nexus-portal",
  "version": "1.0.0",
  "scripts": {
    "start": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && node backend/server.js"
  }
}